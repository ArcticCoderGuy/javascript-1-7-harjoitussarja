// Task 6 – Lisää tekstiä main-elementtiin
const mainElement = document.querySelector(".maincontent");
const textNode = document.createTextNode("Ad astra per aspera");
mainElement.appendChild(textNode);
